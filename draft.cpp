#include <iostream>
#include <fstream>
#include <vector>

int main(){
    // double a = 0.5;
    // int nb_col = 2;
    // int nb_row = 2;

    // std::cout << a << " ";
    // std::cout << std::endl;
    // std::vector<double> elements;
    // elements.resize(2*2);
    // std::cout << elements.size() << std::endl;
    // for (int i = 0; i < elements.size(); i++) {
    //     std::cout << "yes" << std::endl;
    // }
    // // std::cout << sha << std::endl;


    // std::ofstream fout;
    // fout.open("data_draft.txt");
    // fout << 0 << std::endl;
    // fout.close();


    // std::vector <int> a (10);
    // std::cout << a.size() << std::endl;
    if ((1 == 1)&&(2 == 2)){
        std::cout << ((1 == 1)&&(2 == 2)) << std::endl;
    }
}